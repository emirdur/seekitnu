# Node.js dependencies
/node_modules
/pnpm-lock.yaml
/yarn.lock
/package-lock.json

*.local

# TypeScript build output
/dist
/tsconfig.tsbuildinfo
/dist-ssr

# Vite build output
/vite
/.vite

# Docker
/.docker
/docker-compose.yml

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# IDE or editor-specific files
/.idea
/.vscode

# OS-specific files
Thumbs.db

# .env files (Make sure you add these if sensitive data is in them)
.env
.env.local
.env.production
.env.test

# Coverage directory for test results
/coverage

# Optional: If you don't want to commit build artifacts or logs
*.log

# Optional: For Jest testing (if using)
/jest-*.json

# Optional: For storybook if using
/storybook-static

# Optional: TypeScript's `tsc` output (if you build TypeScript separately)
/build

# Ignore all files in `shared` folder if not intended to be committed (you can override this in specific subfolders)
shared/*

# If you want to include some files in the shared folder, you can add exceptions:
!shared/src/*

.vscode/*
!.vscode/extensions.json
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.csv